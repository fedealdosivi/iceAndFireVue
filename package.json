{
  "name": "iceandfire",
  "version": "2.0.0",
  "description": "Nuxt 3 Song of Ice and Fire project",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "node .output/server/index.mjs",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@nuxt/ui": "^3.0.0",
    "nuxt": "^3.16.0"
  },
  "devDependencies": {
    "@nuxt/test-utils": "^3.17.0",
    "@vue/test-utils": "^2.4.0",
    "vitest": "^3.0.0"
  }
}
